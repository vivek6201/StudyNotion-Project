import { Request, Response } from "express";

export const createCourseReviewController = async (
  req: Request,
  res: Response
) => {};

//TODO: make routes for delete and update
export const deleteCourseReviewController = async (
  req: Request,
  res: Response
) => {};

export const updateCourseReviewController = async (
  req: Request,
  res: Response
) => {};

export const getCourseRatingController = async (
  req: Request,
  res: Response
) => {};
