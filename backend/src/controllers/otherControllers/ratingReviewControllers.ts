import { Request, Response } from "express";

export const createCourseReviewController = async (
  req: Request,
  res: Response
) => {};

export const getCourseRatingController = async (
  req: Request,
  res: Response
) => {};
